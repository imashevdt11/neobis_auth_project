server:
  port: 8085
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/neobis_auth
    username: postgres
    password: ronaldinho80
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: u11se03r@gmail.com
    password: tkvb tixr buig chlo
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true